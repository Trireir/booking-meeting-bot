const room = require('./RoomInputField')
const hour = require('./HourInputField')
const date = require('./DateInputField')

module.exports = {
  RoomField: room,
  HourField: hour,
  DateField: date,
}
